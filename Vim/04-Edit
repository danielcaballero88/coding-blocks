# Ingresar al modo insert
i = ingresar al modo insert
a = idem pero adelante del caracter actual 
o = nueva linea por debajo
O = nueva linea por encima 

# Eliminar caracteres
x = delete un caracter bajo el cursor (aprox como la tecla del)
X = idem pero a la izquierda del cursor (aprox como la tecla backspace)
Importante saber que actuan dentro de una linea, no hacen saltos de linea!
r = reemplazar un caracter (debajo del cursor)
d = delete command, se puede combinar con movimientos (ejemplo dw elimina hasta el siguiente comienzo de palabra)
Importante saber que tambien copia el contenido eliminado (entonces actua como cortar ^C+x)
