# Movimiento entre caracteres: h j k l
h = <- (left)
j = v (down)
k = ^ (up)
l = -> (right)

# Movimiento entre palabras: w e b
w = comienzo de siguiente palabra (word)
e = final de siguiente palabra (end)
b = final de palabra anterior (back)

# Se puede combinar con numeros!
3w por ejemplo va 3 veces hasta el comienzo de alguna palabra (probar aca!)

# Moverse en una linea 
0 = ir al comienzo
$ = ir al final

# Moverse en el archivo
gg = comienzo
G = final 
7G = ir a la linea 7

# Moverse entre parentesis que matchean
%
Probar en esta ecuacion (que tiene un parentesis), e incluso (un (parentesis) en [parentesis]).
